25.求下面函数的返回值
int func(int x){
    int countx = 0;
    while(x){
        countx++;
        x = x&(x-1);
    }
    return countx;
}

x&(x-1)的作用是将二进制数从右往左数，遇到的第一个1变成0，右边的所有0变成1，左边的所有数保持不变
200 --> 11001000 
200-1 --> 11000111
200&(200-1) --> 11000000
x&(x-1)的作用是将二进制数从右往左数，遇到的第一个1变成0，右边的数全为0，左边的所有数保持不变
x&(x-1) 将二进制数从右往左数，遇到的第一个1变成0