int main(void){
    int a[5] = {1,2,3,4,5};
    int *p = (int *)(&a+1);
    printf("%d,%d",*(a+1),*(p-1));
    return 0;
}

// 2,5
1)a 和 &a
a:数组名，表示数组首元素地址
&a:数组的地址，表示整个数组的地址
地址值 a == &a
区别：
a:指问的是数组中元素的地址
&a:指向的是整个数组作为一个单元的起始地址
a+1 != &a+1