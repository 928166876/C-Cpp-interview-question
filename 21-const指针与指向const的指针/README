const关键字作用:
1.const定义一个常量，这个常量在定义时必须进行初始化，否则以后就没有机会了
2.实际上const定义的变量并不是一个真正的常量(指针，数组两种方式验证这个观点)
3.const和指针的用法
int const *pt;
const int *pt;  两者相同，都代表pt指针可以指向任意对象，但是不能通过pt指针来修改指向的对象的值

int *const pt; :pt指针不能指向其他位置，但是可以通过pt指针修改指向位置的值
4.const修饰形参