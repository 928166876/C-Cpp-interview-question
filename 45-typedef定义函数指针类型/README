45.请问如下语句是什么意思?
typedef int (*funcptr)();

1) int (*funcptr)();
funcptr:函数指针，指向的函数无参数，并且返回值类型是int类型的函数

2)typedef   //找谁是变量，谁是类型，（typedef之后变量就不是变量了，就变为类型）
typedef int a;
a:类型，int类型
a b; //等价于int b;

typedef int (*funcptr)();
funcptr:类型， 函数指针类型 

3)
funcptr fp1, fp2;
fp1, fp2:函数指针变量
等价于
int (*fp1)();
int (*fp2)();