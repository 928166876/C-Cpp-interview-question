29.给定一个整型变量a，写两段代码，第一个设置a的bit3，第二个清除a的bit3。
```c

1) a = 8;   错误    
2) a = 0;   错误 

知识点:C语言的位操作常见在处理器，特别是寄存器操作
只要出现置1:或操作
只要出现清0:与操作
移位操作: 左移:<< 右移:>>

正确答案:
1) a = a | (1 << 3);    a |= (1 << 3);
2) a = a & ~(1 << 3);   a &= ~(1 << 3);